const DB = function (args) {

  this.getMissionByLaunchDate = (launchDate, next) => {

  };

  this.createNextMission = (args, next) => {

  };

  this.saveAssignment = (args, next) => {

  };

};

module.exports = DB;
